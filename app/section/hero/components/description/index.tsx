import React from "react";
import { motion } from "framer-motion";
import { Animate } from "@/app/animation";

const Description = () => {
  return (
    <p className="w-full max-w-screen-sm font-inter text-[rgb(var(--foreground))] ">
      "As a full-stack developer, I specialize in seamlessly integrating
      front-end and back-end technologies to create cohesive web applications.
      My commitment to bridging the gap between design and functionality ensures
      that users enjoy an intuitive experience while achieving optimal
      performance"
    </p>
  );
};

export default Description;

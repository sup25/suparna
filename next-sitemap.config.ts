import { IConfig } from "next-sitemap";

const config: IConfig = {
  siteUrl: "https://suparna.com.np",
  generateRobotsTxt: true,
  changefreq: "weekly",
  priority: 0.7,
  sitemapSize: 5000,
};

export default config;
